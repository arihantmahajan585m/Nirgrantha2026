# Submission-safe template.
# No paid API keys are configured in this project by default.
# If you later choose to enable a paid provider, add your own local env vars manually.

# Pollinations (free, no key) — Faculty hub "Free AI" mode. Default model is "openai".
# VITE_POLLINATIONS_MODEL=openai
